// Shared fixed height for console section cards (API Keys / Domains / Usage /
// Alerts) so they line up evenly in the 2-up grid. Sized to show ~3 rows;
// content beyond that scrolls inside the card. Tune here to change all cards.
export const CONSOLE_CARD_HEIGHT = 280
